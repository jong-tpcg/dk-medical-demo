export type TranscriptType = {
  speaker: string;
  timecode: string;
  transcript: string;
};
export type MedicalChatMessage = {
  speaker: string;
  time: string;
  message: string;
  sender: string;
};
